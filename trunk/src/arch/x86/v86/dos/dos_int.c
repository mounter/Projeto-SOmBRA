#include <x86/dos.h>
#include <stdio.h>

int dos_int20h(uregs_t *regs)
{
    panic("Chamadas DOS ainda não implementadas!");

    return -1;
}

int dos_int21h(uregs_t *regs)
{
    panic("Chamadas DOS ainda não implementadas!");

    return -1;
}
